# 织梦纪 DreamWeave 项目进度

更新日期：2026-05-26

## 1. 当前阶段

当前项目已经跑通第一版核心线上闭环，进入第一版数据持久化接入和验证阶段。

整体目标仍然是先跑通：

```text
用户输入 → 服务器转发 → 本地 Worker → Ollama 生成 → 返回剧情
```

目前已经完成本地 `Ollama + Python Worker` 基础能力、线上 `apps/server` 部署、第一版 H5 页面、数据库设计和初始化 SQL，以及 `apps/server` 的第一版 PostgreSQL 持久化代码。

公网最小闭环已经跑通：

```text
公网 H5 页面输入
        ↓
线上 Server HTTP 接收请求
        ↓
线上 Server 通过 WebSocket 下发 AI 任务
        ↓
本地 Worker 调用 Ollama
        ↓
Worker 返回 AI 结果
        ↓
线上 Server 返回给 H5 显示
```

当前线上 server 已通过 PM2 启动，`http://47.101.67.241/health` 已验证正常，本地 Worker 已连接线上 `/ws/worker`，`worker_count = 1`。

线上阿里云 Ubuntu 24.04 服务器初始化、代码部署、migration、PM2 启动和公网健康检查已经完成：

```text
基础系统环境 → 部署用户 → 防火墙 → Node.js/PM2 → PostgreSQL → Redis → Nginx → /opt/dreamweave → git clone → migration → PM2 → /health
```

下一步是把本次 PostgreSQL 持久化代码推送到 GitHub，并在服务器 `git pull`、`npm install --omit=dev`、`pm2 restart dreamweave-server --update-env` 后验证消息是否写入数据库。

---

## 2. 已完成内容

### 2.1 项目文档

已完成：

- 整理并修正 `docs/执行框架.md`。
- 明确第一版目标、系统架构、模块职责、核心流程和验收标准。
- 明确本地电脑不暴露公网，Worker 主动连接线上服务器。
- 明确第一版先做 H5，不急着做原生 App。

### 2.2 本地环境

已确认：

- Python 虚拟环境可用。
- PyTorch 可识别 CUDA。
- GPU 可用：NVIDIA GeForce RTX 5060 Ti。
- Ollama 已安装并可运行。
- 本地已有模型：
  - `qwen3:14b`
  - `llama33-novel:latest`

### 2.3 本地 AI Worker

已新增本地 Worker 基础结构：

```text
apps/local-ai-worker/
├── main.py
├── README.md
├── requirements.txt
└── src/dreamweave_worker/
    ├── cli.py
    ├── config.py
    ├── ollama_client.py
    ├── postprocess.py
    ├── prompt.py
    ├── schemas.py
    └── worker.py
```

已支持：

- 命令行健康检查。
- 命令行本地生成测试。
- 调用本地 Ollama HTTP API。
- 流式读取 Ollama 响应。
- 默认模型配置。
- 单次任务覆盖模型。
- 单次任务覆盖部分生成参数。
- 关闭 Qwen3 thinking 输出。
- 模型不存在时返回结构化错误。
- Ollama 超时时返回结构化错误。
- Worker 任务级超时时返回结构化错误。
- 生成内容为空时返回结构化错误。
- 对模型输出做基础清理，去除界面提示、时间戳、Markdown 分隔线等噪声。
- WebSocket connect 模式已按协议补充注册、心跳、任务处理、错误返回、取消任务和断线重连逻辑。
- 已使用本地临时 WebSocket 测试服务器验证 `worker.register → ai.task → ai.result` 链路。

### 2.4 Prompt 与输出质量

已完成：

- 新增共享 Prompt 模板：`packages/prompts/story_continue.txt`。
- 收紧输出规则，只输出小说正文。
- 禁止模型输出“请选择”“我该怎么办”“请直接输入”等界面提示。
- 禁止模型替用户做重大决定。
- 限制模型不要让主角继续执行新的关键动作。
- 允许通过环境变化、NPC 出现、记忆闪回、危险逼近等方式推进剧情。
- 输出长度控制在适合第一版交互的范围内。

当前默认模型：

```text
qwen3:14b
```

默认生成参数：

```text
num_predict = 220
temperature = 0.66
top_p = 0.85
repeat_penalty = 1.08
think = false
```

### 2.5 本地验证

已验证命令：

```powershell
. E:\ai_home\AI_Projects\llm_env\Scripts\Activate.ps1
cd E:\ai_home\AI_Projects\DreamWeave
python .\apps\local-ai-worker\main.py health
python .\apps\local-ai-worker\main.py local "我推开了古堡的大门。" --json
```

验证结果：

- Worker 能连接 Ollama。
- 默认模型 `qwen3:14b` 可用。
- 指定 `llama33-novel:latest` 也可生成。
- 指定不存在模型会返回 `MODEL_NOT_FOUND`。
- 生成结果已经能稳定返回一段较干净的互动小说正文。

### 2.6 最小 Server

已新增最小服务器：

```text
apps/server/
├── package.json
├── README.md
└── src/
    └── index.js
```

已支持：

- `GET /health` 健康检查。
- `GET /workers` 查看已连接 Worker。
- `POST /api/story/continue` 创建小说续写任务。
- `GET /api/sessions/:session_id/messages` 读取会话历史消息。
- `GET /` 托管 H5 页面。
- `ws://127.0.0.1:3000/ws/worker` Worker WebSocket 接入口。
- Worker 注册接收。
- Worker 心跳确认。
- AI 任务下发。
- AI 结果接收。
- AI 错误结果接收。
- Server 等待 Worker 结果超时。
- 超时后向 Worker 发送 `ai.task_cancel`。
- Worker 断开时释放 pending task。
- 通过 `DATABASE_URL` 接入 PostgreSQL。
- 自动读取项目根目录或 `/opt/dreamweave/.env`。
- 请求前自动 upsert `users`、`stories`、`sessions`。
- 保存用户输入到 `messages`。
- 保存 AI 回复到 `messages`。
- 保存并更新 `ai_tasks` 状态、输出、错误和耗时。

当前实现特点：

- 使用 Node.js 内置模块实现 HTTP 和 WebSocket。
- 使用 `pg` 作为 PostgreSQL 客户端。
- 暂不接 Redis / BullMQ。
- pending task 暂存在内存中。

已验证线上闭环：

```text
HTTP POST /api/story/continue
        ↓
Server 下发 ai.task
        ↓
Worker 调用 Ollama
        ↓
Worker 返回 ai.result
        ↓
Server 返回 HTTP success
```

### 2.7 手机端 H5

已新增 H5 页面：

```text
apps/mobile-web/
├── index.html
├── styles.css
├── app.js
└── README.md
```

已支持：

- 访问 `http://127.0.0.1:3000/` 打开页面。
- 查看 Server / Worker 连接状态。
- 自动读取 Worker 可用模型。
- 选择模型。
- 编辑故事标题、世界观和角色设定。
- 输入用户行动。
- 调用 `POST /api/story/continue`。
- 展示用户输入、AI 回复和错误状态。
- 使用 localStorage 暂存本地历史记录。
- 从 `GET /api/sessions/local_session/messages` 读取数据库历史消息。
- 数据库历史不可用时继续使用 localStorage 兜底。

### 2.8 当前运行状态

当前线上服务已运行：

```text
http://47.101.67.241/
http://47.101.67.241/health
```

已验证 `/health` 返回 `status = ok`，本地 Worker 连接后 `worker_count = 1`。

本地 Worker 可通过根目录脚本启动：

```text
start-online-worker.bat
```

如需再次启动本地开发 server：


```powershell
cd E:\ai_home\AI_Projects\DreamWeave\apps\server
npm install
npm run start
```

另开一个 PowerShell 启动 Worker：

```powershell
. E:\ai_home\AI_Projects\llm_env\Scripts\Activate.ps1
cd E:\ai_home\AI_Projects\DreamWeave
python .\apps\local-ai-worker\main.py connect --server ws://127.0.0.1:3000/ws/worker
```

然后访问：

```text
http://127.0.0.1:3000/
```

### 2.9 数据库设计

已新增数据库设计文档：

```text
docs/数据库设计.md
```

已新增数据库初始化脚本：

```text
apps/database/
├── README.md
├── migrations/
│   └── 001_init.sql
└── seeds/
    └── 001_local_dev.sql
```

已设计第一版核心表：

- `users`
- `stories`
- `sessions`
- `messages`
- `ai_tasks`

当前状态：

- 数据库结构已设计。
- PostgreSQL migration SQL 已写好。
- 本地开发 seed 已写好。
- 线上 PostgreSQL 已执行 migration 和 seed。
- `apps/server` 已加入 PostgreSQL 连接和基础持久化代码。
- 当前本地机器未检测到 `psql` 命令，无法在本机直接验证数据库 SQL。
- 持久化代码尚需推送到 GitHub 并在线上 `git pull` 后验证。

### 2.10 线上服务器准备

已购买阿里云 Ubuntu 24.04 服务器：

```text
47.101.67.241
```

已新增初始化命令文档：

```text
docs/服务器初始化命令.md
```

文档覆盖：

- 系统基础更新。
- 部署用户创建。
- 防火墙。
- Node.js 22。
- PM2。
- PostgreSQL。
- Redis。
- Nginx。
- 数据库用户和数据库创建。
- 项目目录。
- migration 执行方式。
- HTTPS 后续配置。
- PostgreSQL 备份脚本。

当前执行进度：

- 已完成服务器购买。
- 已完成 SSH 登录初始化。
- 已执行完成 `docs/服务器初始化命令.md` 第 11 步。
- 已创建 `/opt/dreamweave` 项目目录。
- 已创建线上环境变量文件 `/opt/dreamweave/.env`。
- PostgreSQL、Redis、Nginx、Node.js 22 和 PM2 已按前置步骤安装完成。
- 已将 GitHub 仓库 clone 到 `/opt/dreamweave/current`。
- 已执行线上 PostgreSQL migration 和 seed。
- 已通过 PM2 启动 `dreamweave-server`。
- 已验证 `http://47.101.67.241/health` 返回 `200`。
- 已验证本地 Worker 连接线上后 `worker_count = 1`。

尚未执行：

- 拉取本次数据库持久化代码。
- 在线上执行 `npm install --omit=dev` 安装新增 `pg` 依赖。
- 重启 PM2 进程并验证数据库写入。
- 配置 HTTPS 和域名。
- 做 Worker 鉴权和 SSH 安全加固。

---

## 3. 当前未完成内容

### 3.1 本地 Worker

仍需开发：

- Worker 并发控制的服务端配合。
- Worker 日志。
- Worker 配置文件或 `.env` 自动加载。
- 更完整的错误码体系。

### 3.2 Server

已完成最小线上版本，并已加入第一版 PostgreSQL 持久化代码。

仍需开发：

- 正式框架化，后续可迁移到 NestJS。
- Worker 鉴权。
- 更完整的请求校验。
- 更完整的错误码。
- 更完整的任务状态管理。
- 日志分级和日志文件。
- 后续接入 Redis / BullMQ。
- 多用户、多故事、多会话的正式 API。
- 数据库写入失败时的更完整降级策略。
- 线上部署本次持久化代码后的回归验证。

### 3.3 手机端 H5

已完成最小可用版本。

仍需开发：

- 会话列表。
- 小说项目列表。
- 清空/重命名会话。
- 更完整的移动端适配。
- 更完整的错误提示和重试。
- 多会话、多故事的数据库记录切换。

### 3.4 数据库

已完成设计文档、初始化 SQL 和第一版 server 运行时接入代码。

已完成：

- 线上 PostgreSQL 安装和数据库创建。
- 线上 migration 和 seed 执行。
- `apps/server` 读取 `DATABASE_URL`。
- `apps/server` 请求前自动写入/更新 `users`、`stories`、`sessions`。
- `apps/server` 保存用户输入和 AI 回复到 `messages`。
- `apps/server` 保存 AI 任务状态到 `ai_tasks`。
- `GET /api/sessions/:session_id/messages` 历史消息读取接口。
- H5 默认从 `local_session` 恢复数据库历史消息。

仍需开发：

- 将本次持久化代码推送并部署到线上服务器。
- 在线上验证一次真实生成后数据库写入结果。
- 增加会话列表 API。
- 增加故事列表 API。
- 增加清空/归档会话能力。
- 后续把任务队列接入 Redis / BullMQ。

### 3.5 协议与共享结构

尚未完整抽离。

需要开发：

- `packages/schemas`。
- 统一 AI 任务结构。
- 统一 AI 返回结构。
- 统一错误码。
- 前后端和 Worker 共用的协议结构落地到代码。

已完成文档：

- `docs/Worker通信协议.md`

---

## 4. 建议下一步开发顺序

### 阶段 1：实现 Worker WebSocket 协议

目标：

- 按 `docs/Worker通信协议.md` 实现 Worker 注册消息。
- 实现 Worker 心跳消息。
- 实现 AI 任务接收。
- 实现 AI 结果返回。
- 实现错误返回。
- 实现断线自动重连。

状态：已完成 Worker 侧实现，并已用线上 Server 完成公网闭环验证。

### 阶段 2：实现最小服务器

目标：

- 启动一个 Node.js 服务。
- 提供 Worker WebSocket 接入口。
- 提供前端 HTTP 请求入口。
- 先用内存保存 pending task。
- 能把 HTTP 请求转成 WebSocket 任务发给 Worker。
- 能把 Worker 返回结果返回给 HTTP 请求方。

状态：已完成最小线上版本，已加入第一版 PostgreSQL 持久化代码，暂时未接 Redis / BullMQ。

### 阶段 3：联调 Worker 与服务器

目标：

- Worker 主动连接服务器。
- Worker 注册成功。
- 服务器下发任务。
- Worker 调用 Ollama。
- Worker 返回结果。
- 服务器把结果返回给调用方。
- 断线后 Worker 可以重连。

状态：已完成线上基础联调，本地 Worker 已可连接公网 Server。

### 阶段 4：实现最小 H5 页面

目标：

- 手机网页能输入一句话。
- 页面能显示 AI 回复。
- 页面能显示当前请求状态。
- 页面能选择模型。

状态：已完成最小可用版本，并已加入数据库历史消息读取。

### 阶段 5：接入数据库

目标：

- 保存用户。
- 保存小说项目。
- 保存会话。
- 保存用户输入。
- 保存 AI 回复。
- 保存模型和生成参数。

状态：数据库设计、初始化脚本、线上 migration 和第一版 server 接入代码已完成，等待部署本次代码后做线上写入验证。

已产出：

```text
docs/数据库设计.md
apps/database/migrations/001_init.sql
apps/database/seeds/001_local_dev.sql
```

下一步建议实现：

```text
推送本次持久化代码到 GitHub
服务器 git pull
apps/server 执行 npm install --omit=dev 安装 pg
pm2 restart dreamweave-server --update-env
验证 /health 中 database.connected = true
通过 H5 生成一次内容
用 psql 查询 messages 和 ai_tasks 是否写入
继续开发会话列表和故事列表
```

---

## 5. 当前关键决策

### 5.1 模型选择

模型不应写死在 Worker 中。

当前规则：

```text
任务 model 字段优先
        ↓
Worker 默认模型兜底
        ↓
模型不存在则返回 MODEL_NOT_FOUND
```

这意味着后续可以按用户、故事、会话或前端选择动态切换模型。

### 5.2 第一版任务队列

第一版可以先用内存 pending task 跑通闭环。

Redis / BullMQ 建议在核心链路跑通后接入。

### 5.3 第一版数据库

数据库不必阻塞 WebSocket 联调。

建议先跑通：

```text
HTTP 请求 → WebSocket Worker → Ollama → HTTP 返回
```

再补：

```text
保存用户输入和 AI 回复
```

### 5.4 Prompt 策略

第一版 Prompt 先追求稳定输出，不追求复杂 Agent。

当前重点：

- 输出干净。
- 不替用户做决定。
- 保持剧情推进。
- 方便前端直接展示。

---

## 6. 第一版验收清单

### 6.1 已完成

- 本地 Worker 可以长期运行。
- Worker 可以主动连接服务器。
- Worker 断线后可以自动重连。
- 服务器可以接收前端请求。
- 服务器可以创建 AI 任务。
- 服务器可以把任务发给 Worker。
- Worker 可以调用 Ollama 生成小说正文。
- Worker 可以返回成功或错误结果。
- 前端可以展示 AI 回复。
- 手机端完成一次完整互动。
- 线上 Server 已部署并通过公网 `/health` 验证。
- 本地 Worker 已连接线上 Server。
- 数据库 migration 和 seed 已在线上执行。
- Server 已加入第一版 PostgreSQL 持久化代码。
- H5 已加入数据库历史消息读取代码。

### 6.2 剩余

- 将本次持久化代码部署到线上并验证真实写入。
- 数据库可以保存多用户、多故事、多会话记录。
- 页面支持会话列表和故事列表。
- 页面支持清空、归档或重命名会话。

---

## 7. 当前风险

### 7.1 模型输出仍需持续调优

Prompt 已经明显收紧，但不同模型仍可能出现风格、长度、动作边界不稳定的问题。

后续需要在真实交互中持续调整 Prompt、生成参数和后处理规则。

### 7.2 任务可靠性尚未完成

当前还没有持久任务队列。

如果服务器或 Worker 中途断开，任务恢复、重试和状态追踪还需要后续实现。

### 7.3 数据库持久化尚未线上回归

当前代码已经接入 PostgreSQL 持久化，但本次改动还没有推送并部署到线上服务器。

第一版验收前需要在线上完成一次真实生成，并用 SQL 验证 `messages` 和 `ai_tasks` 已写入。

当前机器未检测到 `psql` 命令，因此本地无法直接验证数据库写入，需要在服务器上验证。
